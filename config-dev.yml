server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/kartian/springcloud-config
#          uri: https://github.com/Kar5873/springcloud-config
#          skipSslValidation: true
#          search-paths: '{application}'
#          default-label: master
#      label: master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
